int function factorial(n : int)
begin
    if (n = 0) then
        return (n * factorial(n-1));
    else
        return 1;
    endif;
end;

void main()
begin
    begin
        printi(factorial(5));
    end;
end;
